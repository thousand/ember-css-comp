{
  "name": "ember-css-comp",
  "version": "0.0.1",
  "description": "Three ember apps with lots of components to test build and run time perf of css options",
  "main": "index.js",
  "author": "Bryan Levay <bryan@bryanlevay.com>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "proliferate": "^0.0.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "node gen-content.js",
    "degenerate": "find packages -name *component*.hbs -delete && find packages -name *component*.scss -delete"
  }
}
